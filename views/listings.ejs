<% layout("/layouts/boilerplate") %>

<body>
    <h2 class="poppins-semibold">
        Our available listings
    </h2>

    <ul>
        <% for(let listing of allListings){ %> 
            <li class="poppins-regular">

                <a href="/listings/show/<%= listing._id %>">
                    <%= listing.title %>
                </a>

            </li>   
        <% } %>
    </ul>

</body>